# Debug symbols

`ScreenshotLightingAssistant.pdb` is intended for crash-log symbolication and development. It is not required to install or run the mod.

Do not install the Symbols ZIP with MO2. Keep it with the matching 0.1.29 DLL when investigating a crash; symbols from a different build may report incorrect function names or locations.
