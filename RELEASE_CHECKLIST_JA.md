# Screenshot Lighting Assistant 0.2.0 実機確認

0.2.0正式版の常用ライト機能は、以下をすべて合格したbeta4と同じコードです。正式版では入口の「F」だけを人型アイコンへ変更しました。旧版のMODとZIP、`Overwrite/SKSE/Plugins/ScreenshotLightingAssistant/` をバックアップしてから、DLLが二重にならない形で導入します。

## 正式版アイコン差し替え（公開前の最終確認）

1. 画面下が `0.2.0`、上部が `[P] [人型アイコン] [EN/JP]` の並びになっていることを確認します。
2. 上部の人型アイコンを押すと常用フェイスライト窓が開き、機能とプレイヤーライトがONのとき橙色になることを確認します。
3. 円形UI内の人型ボタンは従来どおり撮影用フェイスライトのON/OFFとして動き、隣の設定ボタンも変わっていないことを確認します。

## 常用フェイスライト本体（beta4で2026-09-10実機確認済み）

1. beta3を無効にしてbeta4を導入し、画面下が `0.2.0-beta4`、上部が `[P] [F] [EN/JP]` の並びであることを確認します。
2. Fを開き、先頭に「常用フェイスライト機能を使用」があり、従来の「配置基準」とゲームパッドの長い説明文がなくなっていることを確認します。
3. 機能と「プレイヤー」をONにします。状態が「ON：プレイヤーへ追従しています。」となり、頭の向きを変えても顔の前方へ追従することを確認します。
4. ライトが点いた状態で機能マスターだけをOFFにします。直ちに消灯し、「機能OFF：ホットキーは反応しません。」と表示され、Fの橙色も消えることを確認します。
5. 機能OFF中に登録済みのキーボードを押す、またはゲームパッドを約0.6秒長押ししても反応しないことを確認します。登録名・強さ・範囲・上下・プレイヤーON状態は変わらないことも確認します。
6. 機能を再びONにすると、保持されていたプレイヤーON状態と調整値で1灯だけ復帰することを確認します。
7. プレイヤーだけをOFFにして「待機：プレイヤーライトはOFFです。」、強さ0で「待機：強さが0です。」と表示されることを確認します。
8. プレイヤー撮影で撮影用フェイスライトをONにし、「撮影用フェイスライトを一時的に使用中です。」と表示され、二重照明にならず、撮影停止後に常用設定へ戻ることを確認します。
9. beta3以前の `persistent-face.sla` を残して起動し、機能マスターがON、キーボード／ゲームパッド・ライトON/OFF・強さ・範囲・上下が引き継がれることを確認します。従来カメラ基準だった場合も、0.2.0では顔向き基準へ移行するのが正しい動作です。
10. 機能マスターを任意の状態にしてSkyrimを終了・再起動し、その状態が保存されることを確認します。最後にロード／屋内外移動でも残光、CTD、点滅、二重照明がないことを確認します。

## 0.1.29正式版で確認済みの項目（回帰確認）

## 新規導入

1. Skyrimを完全に終了します。
2. My Games側とMO2 Overwrite側の既存 `ScreenshotLightingAssistant` フォルダーを、削除せず別名へ変更します。
3. MO2へ `ScreenshotLightingAssistant-0.1.29-MO2.zip` だけを導入します。
4. SKSE経由で起動し、画面下の表示が `0.1.29`、初期UIが英語であることを確認します。
5. 撮影ライトを開始し、通常3灯とフェイスライトを一度操作します。
6. ライトとプリセットを各1件登録し、お気に入り・言語を変更します。
7. `Overwrite/SKSE/Plugins/ScreenshotLightingAssistant/` が新しく作成されることを確認します。
8. Skyrimを完全に終了して再起動し、登録・お気に入り・言語が残ることを確認します。

## 「初期値」の確認（RC2で合格済み）

1. 通常ライトを選び、種類・色・配置方向・影方式・点灯状態を分かりやすく変更します。
2. 強さ・範囲・距離・高さ・3軸の微調整・影の補正を初期値以外へ変更します。
3. 「3. 基本設定」の横にある「初期値」（英語では「Reset values」）を押します。
4. 強さ1.00、範囲4.0m、距離2.20m、高さ0.00m、微調整3軸0.00m、影の補正1（標準）へ戻ることを確認します。
5. 配置方向・種類・色・影方式・点灯状態が変わらないことを確認します。
6. 「戻る」で全調整値が一度に復元され、「進む」で再び初期値になることを確認します。
7. 初期値のライトと未配置スロットではボタンが無効であること、狭い幅と日英表示で重ならないことを確認します。

## 旧版からの更新

新規導入確認後、必要なら退避した旧フォルダーを使って別途確認します。元データのバックアップを残したまま、旧My Games保存から新しいOverwrite保存へ一度だけコピーされること、旧側が削除されないことを確認します。

## 合格条件

- 起動時にSKSEのDLLエラーが出ない
- Mod Control PanelへLightingが表示される
- ライトが点灯し、メニューを閉じても維持される
- 保存データが再起動後も残る
- Source ZIPとSymbols ZIPをMO2へ導入していない

異常があった場合は、退避したフォルダーをそのまま残し、`skse64.log` と `ScreenshotLightingAssistant.log` を確認してください。
